apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler

metadata:
  name: hello-nginx-hpa
  namespace: project1

spec:

  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: hello-nginx

  minReplicas: 2

  maxReplicas: 10

  metrics:

  - type: Resource

    resource:
      name: cpu

      target:
        type: Utilization
        averageUtilization: 50