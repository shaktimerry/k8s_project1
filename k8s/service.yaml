apiVersion: v1
kind: Service
metadata:
  name: hello-nginx-service
  namespace: project1

spec:
  selector:
    app: hello-nginx

  type: NodePort

  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080